{
  "name": "trac-nghiem-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo root - proxy scripts cho Render (vì Render auto-detect Node từ package.json ở root và tự chạy 'yarn start')",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "cd backend && npm install --omit=dev && node index.js",
    "build": "cd backend && npm install --omit=dev",
    "postinstall": "cd backend && npm install --omit=dev"
  }
}
